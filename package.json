{
  "name": "Boilerplate v3.0",
  "version": "1.0.0",
  "description": "Boilerplate for new Eleventy websites using Eleventy 3.0 features.",
  "main": "index.js",
  "scripts": {
    "clean": "npx del public",
    "start": "eleventy --serve",
    "build": "eleventy && npx pagefind --site public",
    "prestart": "npm run clean && npx lightningcss --minify css/main.css -o css/main.min.css && npx esbuild js/main.js --bundle --minify --outfile=js/main.min.js",
    "prebuild": "npm run clean && npx lightningcss --minify css/main.css -o css/main.min.css && npx esbuild js/main.js --bundle --minify --outfile=js/main.min.js"
  },
  "type": "module",
  "author": "George Braden",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^4.0.2",
    "animejs": "^4.3.5",
    "decap-cms": "^3.6.4",
    "del-cli": "^5.0.0",
    "dotenv": "^16.0.3",
    "file-system": "^2.2.2",
    "markdown-it-attrs": "^4.1.6",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "esbuild": "0.25.5",
    "lightningcss-cli": "^1.29.3"
  }
}
